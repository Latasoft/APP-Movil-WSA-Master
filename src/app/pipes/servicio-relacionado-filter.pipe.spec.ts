import { ServicioRelacionadoFilterPipe } from './servicio-relacionado-filter.pipe';

describe('ServicioRelacionadoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicioRelacionadoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
